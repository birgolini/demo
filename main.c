#include <studio.h>

int main() {
	printf("Heiheihei\n")
    printf(" onsdager er kult\n");
    
    if(1 < 0) {
        // In C, returning something non-zero signals an error
        return 1;
    }

	return 0;
}
